# Identifiants MinIO utilisés par collect.py pour écrire sur le bucket bronze.
# Doivent correspondre à infra/minio/.env (MINIO_ROOT_USER / MINIO_ROOT_PASSWORD).
MINIO_ACCESS_KEY=ev_admin
MINIO_SECRET_KEY=change_me_avant_le_deploiement
