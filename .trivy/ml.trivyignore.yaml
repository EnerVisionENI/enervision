# Exceptions Trivy de l'image ml — appliquées uniquement à l'étape de barrière du job
# `build` (voir .github/workflows/ci-cd.yml). Le rapport JSON archivé en artefact, lui, est
# produit SANS ce fichier : il continue de lister l'intégralité des vulnérabilités.
#
# Vide depuis le passage de ml/requirements.txt à `mlflow-skinny` (ni serveur ni UI mlflow
# embarqués dans l'image) : les CVE du serveur mlflow ne s'appliquent plus, et `pyarrow` n'est
# plus plafonné à <20 par mlflow, ce qui laisse passer le correctif 23.0.1+. Voir l'historique
# git de ce fichier pour le détail des CVE précédemment exceptées et le raisonnement complet.
#
# A garder vide : toute nouvelle exception doit être justifiée au cas par cas, avec une
# `expired_at`, comme précédemment.

vulnerabilities: []
